from pathlib import Path

from docx import Document


SRC = Path(r"C:\Users\hasee\Desktop\paper_revised_sec_rgcl.docx")
OUT = Path(r"C:\Users\hasee\Desktop\paper_polished_sec_rgcl.docx")


def set_text(p, text):
    p.clear()
    p.add_run(text)


def replace_first(doc, prefix, text):
    for p in doc.paragraphs:
        if p.text.strip().startswith(prefix):
            set_text(p, text)
            return True
    raise ValueError(f"missing paragraph prefix: {prefix}")


def main():
    doc = Document(str(SRC))

    set_text(doc.paragraphs[0], "面向中文有害 Meme 检测的解释增强反事实检索对比学习")
    set_text(
        doc.paragraphs[1],
        "Explanation-Enhanced Counterfactual Retrieval-Guided Contrastive Learning for Chinese Harmful Meme Detection",
    )

    replace_first(
        doc,
        "随着社交媒体和生成式大模型的快速发展",
        "图像与文本共同构成的 Meme 已成为社交媒体中传播观点、情绪和群体态度的重要载体，也为有害内容的隐蔽表达提供了空间。中文有害 Meme 往往依赖谐音、网络用语、文化典故以及图文组合后的隐含语义来表达侮辱、歧视或攻击意图。现有 CLIP 类视觉语言模型和检索引导对比学习方法能够缓解部分混淆样本问题，但它们主要依赖原始图像与 OCR 文本的隐式表示，难以稳定捕获中文语境中的深层语义证据。",
    )
    replace_first(
        doc,
        "针对上述问题，本文提出 SEC-RGCL",
        "针对这一问题，本文提出 SEC-RGCL，一种语义解释与反事实证据增强的检索引导对比学习框架。该方法首先利用多模态大模型生成中性的语义解释句，将图像、OCR 文本及其组合语义显式化；随后通过上下文感知门控、FiLM 调制和语义残差连接，将解释信息以受控方式注入图文联合表示，避免解释噪声直接主导分类结果。在此基础上，本文根据分类器权重和样本表示激活估计关键证据维度，并构造证据削弱后的反事实表示，以约束模型依赖稳定且具有判别意义的语义线索。",
    )
    replace_first(
        doc,
        "本文在 ToxiCN MM 中文有害 Meme 数据集上进行实验",
        "本文在 ToxiCN MM 中文有害 Meme 数据集上进行实验，并以 RGCL 为主要基线开展整体性能比较、模块消融、辅助文本对比和反事实响应分析。实验结果表明，SEC-RGCL 在 Acc 和 AUC 上分别达到 0.8392 和 0.8622，较原始 RGCL 基线提升 2.75 和 2.71 个百分点。进一步分析显示，语义解释比重复输入 OCR 文本提供了更有效的辅助语义信息，反事实证据学习则增强了模型对关键判别维度的响应一致性。",
    )

    replace_first(
        doc,
        "随着社交媒体平台的快速发展",
        "社交媒体平台的快速发展使 Meme 成为网络交流中高度活跃的多模态表达形式。Meme 通常将图像、文字和共享文化背景压缩在同一表达单元中，能够以幽默、讽刺或隐喻的方式传播态度和立场。与此同时，这种表达形式也为有害内容提供了隐蔽载体。与纯文本有害言论不同，有害 Meme 的攻击性含义往往并不完全出现在单一模态中，而是由视觉元素、OCR 文本以及二者之间的语用关系共同触发。因此，构建能够理解图文组合语义的自动检测方法，是在线内容治理和社区安全中的重要问题。",
    )
    replace_first(
        doc,
        "准确检测有害 Meme 的核心困难之一",
        "有害 Meme 检测的一个核心困难是混淆样本（confounder memes）。这类样本在图像或文字层面可能高度相似，但由于图文组合关系不同，其整体语义和类别标签会发生显著变化。例如，一个有害 Meme 可能与良性 Meme 共享相同文本，仅因配图不同而产生攻击性含义；也可能共享相同图像，仅因文字变化而从攻击性表达转变为无害表达。由于表层相似性会使这些样本在视觉语言表示空间中相互接近，模型需要识别的并不是单个视觉或文本线索，而是图文共同构成的判别性语义。",
    )
    replace_first(
        doc,
        "现有研究主要从两条路径改进该问题",
        "已有研究主要从两条路径缓解这一问题。第一类方法设计跨模态融合模块，以增强图像与文本之间的对齐；第二类方法通过检索增强对比学习，动态选择同类伪正样本和异类困难负样本，从而学习更具判别性的表示空间。然而，在中文有害 Meme 场景中，许多关键线索来自谐音、典故、网络亚文化和语境暗示。仅依赖原始图像与 OCR 文本的隐式编码，模型仍可能受到浅层相似性、数据偏差或偶然相关特征的影响，难以稳定捕获真正支持有害性判断的语义证据。",
    )
    replace_first(
        doc,
        "为此，本文提出 SEC-RGCL，将显式语义解释",
        "为此，本文提出 SEC-RGCL，将显式语义解释和反事实证据约束引入检索引导对比学习框架。与直接让大模型输出有害/无害标签不同，本文要求多模态大模型生成一条中性英文解释句，用于描述图像与 OCR 文本共同传达的具体含义，而不包含道德判断或分类标签。该解释句作为额外语义信号参与表示学习，使模型能够获得原始编码器难以显式表达的文化背景、隐喻关系和图文组合语义，同时降低标签泄漏和过度推断风险。",
    )
    replace_first(
        doc,
        "进一步地，本文引入归因引导的反事实证据学习机制",
        "进一步地，本文引入归因引导的反事实证据学习机制，用于检验并约束模型是否依赖有效的判别证据。模型根据最终表示维度的激活值和分类头权重估计关键证据维度，并对这些维度进行削弱以构造反事实表示。对于有害样本，如果关键维度确实承载了有害性证据，削弱后有害类别置信度应当下降；对于无害样本，同类扰动不应异常提高有害类别置信度。该约束使模型在训练过程中更倾向于利用稳定证据，而不是局部高频词、视觉 shortcut 或伪相关模式。",
    )
    replace_first(
        doc,
        "本文以 RGCL 为基础骨架",
        "本文以 RGCL 为基础骨架，在语义增强后的表示空间中执行检索与对比学习。语义解释并不替代原始图文特征，而是通过门控、调制和残差机制对图文表示进行补充与校准；检索机制则继续承担拉近同类样本、拉远异类混淆样本的作用。通过二者结合，模型既保留 RGCL 对混淆样本的判别优势，又获得面向中文语境的显式语义补充。",
    )
    replace_first(
        doc,
        "本文在 ToxiCN MM 数据集上验证所提出方法",
        "本文在 ToxiCN MM 数据集上验证所提出方法，并围绕整体性能、模块消融、辅助文本类型和反事实响应进行分析。实验结果显示，语义解释增强和反事实证据学习均能提升 RGCL 基线性能，且二者联合使用时取得最佳结果。",
    )

    replace_first(
        doc,
        "有害 Meme 检测要求模型同时理解视觉内容",
        "有害 Meme 检测要求模型同时理解视觉内容、图中文字以及二者组合后的语义。早期方法通常依赖目标检测器或视觉语言预训练模型提取多模态特征，并通过跨模态注意力、特征拼接或逐维交互完成分类。近年来，CLIP 类模型因端到端结构简洁、图文对齐能力较强而被广泛用于 Meme 检测任务。然而，已有研究表明，CLIP 表示空间对 confounder memes 中细微但关键的图文差异仍不够敏感，容易将表层相似但语义标签相反的样本映射到邻近区域。",
    )
    replace_first(
        doc,
        "中文有害 Meme 的语义理解高度依赖文化背景",
        "中文有害 Meme 的语义理解高度依赖文化背景、谐音、网络流行语和隐喻表达。参考论文中的 FG-E2HMD 强调，先生成文化感知解释再进行分类，有助于提升检测性能和决策透明度。本文借鉴这一“先解释、再利用解释增强判断”的思想，但不直接使用解释生成标签，而是将中性解释句作为表征学习的辅助语义信号。这一设计使解释能够补充中文文化语义，同时避免将大模型的主观判断直接转化为分类依据。",
    )
    replace_first(
        doc,
        "RGCL 通过动态检索伪正样本和困难负样本",
        "RGCL 通过动态检索伪正样本和困难负样本，构造 hatefulness-aware embedding space，是本文的主要 baseline。该方法能够缓解混淆样本在表示空间中过度接近的问题，但其检索仍主要依赖原始图文表示。本文在 RGCL 基础上引入语义解释增强的检索空间，并进一步加入反事实证据学习，使模型不仅学习更清晰的类间结构，也受到关键证据依赖关系的显式约束。",
    )

    replace_first(
        doc,
        "本文提出语义解释与反事实证据增强的检索引导对比学习框架",
        "本文提出语义解释与反事实证据增强的检索引导对比学习框架（Semantic Explanation and Counterfactual Evidence Enhanced RGCL，SEC-RGCL）。该框架以 RGCL 的图文联合表示和动态检索机制为基础，加入语义解释生成与受控融合模块，并通过反事实证据学习约束模型对关键判别维度的依赖。",
    )
    replace_first(
        doc,
        "原始 RGCL 通过动态检索 pseudo-gold positive",
        "原始 RGCL 通过动态检索 pseudo-gold positive 和 hard negative，在图文联合表征空间中拉近同类样本、拉远异类混淆样本，从而学习具有有害性判别能力的表示空间。该机制适合处理图像或文本相似但标签相反的混淆样本。然而，在中文有害 Meme 场景下，关键语义常来自网络用语、文化典故或图文组合后的隐含含义，仅依赖原始图文 embedding 可能无法提供足够稳定的判别依据。",
    )
    replace_first(
        doc,
        "SEC-RGCL 的核心思想是",
        "SEC-RGCL 的核心思想是将隐式图文语义显式化，并进一步约束模型对该语义证据的使用方式。具体而言，框架首先将 Meme 的图文组合语义生成一条中性解释句，再将该解释作为辅助语义信号注入图文表示；随后在语义增强表示空间中执行检索引导对比学习，并通过反事实扰动约束模型的证据依赖关系。语义解释模块主要回答“模型可以获得什么额外语义信息”，反事实证据模块则进一步回答“模型是否真正依赖这些有效证据进行判断”。",
    )
    replace_first(
        doc,
        "本文中的语义解释并非用于直接输出 harmful",
        "本文中的语义解释并非用于直接输出 harmful 或 non-harmful 的最终判断，而是被设计为一条用于 meme embedding 的中性英文语义解释句。该解释句概括 Visual + OCR 共同传达的具体含义，重点包括图像中的关键实体、OCR 文本的字面含义以及二者组合后的语义关系。在证据充分时，解释句还可以包含中文网络用语、谐音、文化引用、符号和群体指代等信息。与标签式解释不同，该设计降低了标签泄漏风险，使解释信息更适合作为后续融合、检索和对比学习的语义补充。",
    )
    replace_first(
        doc,
        "该提示词具有两个方面的作用",
        "该提示词具有两个作用。首先，它要求大模型以一句中性英文语义句概括图像和 OCR 共同传达的具体含义，从而将隐式跨模态语义转化为可编码的文本信息。其次，它明确禁止模型输出有害/无害标签、道德判断以及缺乏证据的意图描述，使生成内容不直接替代分类器的判别过程，而仅作为原始图文表征的补充信号。当 meme 的隐含含义较为明确时，解释句可以提供关于中文网络用语、文化引用和图文组合语义的额外信息；当隐含含义不明确时，解释句仅保留图像和 OCR 的字面描述，以降低无依据推断带来的噪声。",
    )
    replace_first(
        doc,
        "为有效利用语义解释信息，本文并不直接将",
        "为有效利用语义解释信息，本文并不将解释特征与图文特征简单拼接。简单拼接会使解释中的冗余信息或生成噪声被无差别地引入模型，进而影响表示稳定性。因此，本文设计解释门控机制，根据当前图文表示动态控制解释信息的注入强度：",
    )
    replace_first(
        doc,
        "通过上述设计，解释信息并非作为独立判断依据",
        "通过上述设计，解释信息并非作为独立判断依据来替代原始图文特征，而是在统一隐空间中对图文表示进行补充和校准。该过程保留了图像与 OCR 文本的原始跨模态证据，同时引入关于图文组合含义、网络用语和文化引用的高层语义信息。",
    )
    replace_first(
        doc,
        "虽然语义解释增强能够为模型提供额外的高层语义信息",
        "虽然语义解释增强能够为模型提供额外的高层语义信息，检索引导对比学习也能够优化 embedding 空间的类间结构，但这并不保证模型在分类时真正依赖稳定且有效的判别证据。模型仍可能基于局部高频词、视觉 shortcut 或训练数据中的偶然相关模式完成预测。为缓解这一问题，本文进一步提出反事实证据学习模块，在最终分类 embedding 上构造证据削弱后的反事实表示，并约束原始预测与反事实预测之间的关系。",
    )
    replace_first(
        doc,
        "该模块的目的并非简单地进行特征扰动",
        "该模块的目的并非简单地进行特征扰动，而是在表示空间中构造关键证据被削弱的反事实情形。通过这种方式，模型被显式约束为：当有害样本的关键证据被削弱时，toxic 预测应相应下降；当无害样本受到同类扰动时，模型不应产生异常的 toxic 响应。该机制有助于提升模型决策依据的稳定性，并为后续分析模型内部响应提供可操作的检验方式。",
    )

    replace_first(
        doc,
        "本章对 SEC-RGCL 进行实验评估",
        "本章对 SEC-RGCL 进行实验评估。首先介绍数据集、模型设置、评价指标和参数设置；随后比较 SEC-RGCL 与原始 RGCL 基线的整体性能，并通过模块消融分析语义解释增强与反事实证据学习的独立贡献；最后进一步考察语义解释相对于原始 OCR 文本的作用，以及反事实证据学习对模型内部响应的影响。",
    )
    replace_first(
        doc,
        "本文所有实验均在 ToxiCN MM 数据集上进行",
        "本文所有实验均在 ToxiCN MM 数据集上进行。ToxiCN MM 是面向中文有害 meme 检测的大规模细粒度标注数据集，共包含 12,000 个样本。本文采用数据集的官方划分方式，将全部样本划分为训练集、验证集和测试集，比例分别为 80%、10% 和 10%，对应样本数量为 9,600、1,200 和 1,200。",
    )
    replace_first(
        doc,
        "为分析不同组成部分对模型性能的影响",
        "为分析不同组成部分对模型性能的影响，本文在原始 RGCL 基础上构建多个变体，并与完整 SEC-RGCL 进行比较。Baseline 表示原始 RGCL；Baseline + Exp 表示加入语义解释生成、编码与融合模块；Baseline + CF 表示加入反事实证据学习模块；Baseline + Text 用原始 OCR 文本替代语义解释，以控制额外文本分支带来的结构性影响；SEC-RGCL 表示同时使用语义解释增强和反事实证据学习的完整模型。各模型的具体设置如表 4-1 所示。",
    )
    replace_first(
        doc,
        "与 Baseline 相比，SEC-RGCL 的 Acc",
        "与 Baseline 相比，SEC-RGCL 的 Acc 由 0.8117 提升至 0.8392，提高 2.75 个百分点；AUC 由 0.8351 提升至 0.8622，提高 2.71 个百分点。两项指标上的一致增益表明，在原始 RGCL 框架中同时引入语义解释增强与反事实证据学习，能够改善中文有害 Meme 的分类性能。由于 AUC 衡量不同阈值下的整体排序能力，该结果也说明性能提升并非仅来自某一固定分类阈值的偶然变化。",
    )
    replace_first(
        doc,
        "完整 SEC-RGCL 在两项指标上均取得最优结果",
        "完整 SEC-RGCL 在两项指标上均取得最优结果。相较于 Baseline + Exp，SEC-RGCL 的 Acc 和 AUC 进一步提高 1.17 和 0.96 个百分点；相较于 Baseline + CF，分别提高 2.17 和 2.06 个百分点。上述结果表明，语义解释增强与反事实证据学习具有互补性：前者扩展模型可利用的显式语义信息，后者约束模型对判别证据的依赖方式，二者结合能够带来更稳定的性能收益。",
    )
    replace_first(
        doc,
        "由于 Baseline + Text 与 Baseline + Exp 均保留额外文本编码",
        "由于 Baseline + Text 与 Baseline + Exp 均保留额外文本编码和融合路径，二者之间的性能差异主要反映辅助文本内容本身的作用。相较于重复输入原始 OCR 文本，语义解释能够概括图像与文字共同表达的含义，并补充原始文本中未显式表达的语义关系，因此为分类模型提供了更有效的辅助信息。需要强调的是，该实验评价的是解释作为模型输入的贡献，而不是解释文本本身的人类可读质量。",
    )
    replace_first(
        doc,
        "结合消融实验结果，CF 模块不仅提高了 Acc",
        "结合消融实验结果，CF 模块不仅提高了 Acc 和 AUC，也增强了模型在关键证据受到干预时的响应一致性。需要说明的是，该分析刻画的是模型内部高贡献维度与预测结果之间的关联，不能据此直接断言这些维度具有明确的人类语义解释。换言之，反事实证据学习提供的是一种表示层面的稳定性约束，而不是完整的因果解释。",
    )

    replace_first(
        doc,
        "为进一步说明 SEC-RGCL 的作用",
        "为进一步说明 SEC-RGCL 的作用，本文选取两个原始 RGCL 与通用大模型均容易误判的样本进行分析。案例分析并不替代定量实验，而是用于展示语义解释如何补充图文组合含义，以及反事实证据约束如何帮助模型减少对表层线索的依赖。",
    )
    replace_first(
        doc,
        "案例 1 中，Meme 使用“垃圾袋”",
        "案例 1 中，Meme 使用“垃圾袋”这一视觉隐喻讽刺对方“能装”，其攻击性并不完全来自 OCR 文本本身，而是来自图像符号与中文网络语义之间的组合关系。语义解释将该隐含关系显式化后，SEC-RGCL 能够更好地区分表面无害表达与实际讽刺意图。",
    )
    replace_first(
        doc,
        "案例 2 中，亲吻场景",
        "案例 2 中，亲吻场景与“科学家正在努力治愈她们”的文字共同构成对性少数群体的病理化隐喻。该样本的有害性来自图文组合后的社会语义，而非单一词语触发。SEC-RGCL 通过解释增强获得更明确的语义补充，因此能够识别原始图文表示中不易捕获的歧视性含义。",
    )

    replace_first(
        doc,
        "本文提出 SEC-RGCL，一种面向中文有害 Meme 检测",
        "本文提出 SEC-RGCL，一种面向中文有害 Meme 检测的语义解释与反事实证据增强检索对比学习方法。该方法在 RGCL 的动态检索对比学习框架基础上，引入多模态大模型生成的中性语义解释，并通过门控、FiLM 调制和残差连接将解释信息受控注入图文表示。同时，本文设计反事实证据学习模块，通过削弱高贡献维度并约束预测响应，促使模型依赖更稳定的判别证据。",
    )
    replace_first(
        doc,
        "在 ToxiCN MM 数据集上的实验表明",
        "在 ToxiCN MM 数据集上的实验表明，SEC-RGCL 在 Acc 和 AUC 上均优于原始 RGCL。消融实验验证了语义解释增强和反事实证据学习的独立贡献；辅助文本对比说明，语义解释相较于重复输入 OCR 文本能够提供更有效的补充信息；反事实响应分析则表明，完整模型对关键证据维度具有更一致的预测变化。",
    )
    replace_first(
        doc,
        "本文仍存在若干限制",
        "本文仍存在若干限制。首先，语义解释质量依赖底层多模态大模型，当模型缺乏特定文化知识或产生过度推断时，解释可能引入噪声。其次，反事实证据学习在表示维度上进行干预，不能直接等同于人类可解释的语义因果关系。未来工作可进一步研究可验证的解释生成、跨数据集泛化以及更细粒度的人类可读证据定位。",
    )

    doc.save(str(OUT))
    print(f"wrote {OUT}")


if __name__ == "__main__":
    main()
